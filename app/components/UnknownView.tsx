import React from 'react'
import { View } from 'react-native'
import { styles } from './Styles.ts'

const UnknownView: React.FC = () => {
  return (
        <View style={styles.unknownView}/>
  )
}

export default UnknownView
